*****

**查询字段未遵循最左前缀原则**

**join两边字符集不一致**

**where 用函数**

**where隐式类型转换**

**or条件未索引覆盖,可能放弃使用索引**

**like  左%**



 **order by / group by 未包含在索引中 可能失效**



**数据分布不均匀,索引列重复率高,可能放弃索引全表扫描**



**尽量减少使用 *!=*、*<>*、*IS NULL*、*NOT IN* 等操作。**